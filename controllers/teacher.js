const teacherModal = require('../modals/teacher')
const mongoose = require('mongoose')


const getTeacherList = async (req , res) => {
    try {

    }catch (error){
        console.log(error)
        res.send()
    }
}

module.exports = getTeacherList;